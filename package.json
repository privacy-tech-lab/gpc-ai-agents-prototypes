{
  "name": "gpc-ai-agents-prototypes",
  "version": "1.0.0",
  "description": "GPC enforcement in AI agent pipelines. Eight prototypes, numbered 1 through 8, each testing one opt-out enforcement mechanism or typology category.",
  "scripts": {
    "compare":        "node compare_architectures.js",
    "compare-run":    "node compare_architectures.js --run",
    "demo-a":         "npm run demo --prefix prototype-1",
    "demo-b":         "npm run demo --prefix prototype-2",
    "signal-drop-a":  "npm run signal-drop --prefix prototype-1",
    "signal-drop-b":  "npm run signal-drop --prefix prototype-2",
    "test-a":         "npm test --prefix prototype-1",
    "test-b":         "npm test --prefix prototype-2"
  },
  "license": "ISC"
}
